/* 
-> Tuples are basically static typed arrays. 
- this means that the types of the members of an array at a certain positin cannot be changed. 
*/

let tup: [string, string, number, boolean]

tup = ['paul', 'onochie', 25, true]